%h1 Listing Members
%table
  %tr
    %th Name
    %th Username
    %th Last Login
    %th Number of games
  - @users.each do |user|
    %tr
      %td= link_to user.long_name, edit_admin_user_path(user)
      %td= h user.username
      %td= user.last_login_at
      %td= user.games.count
%br/
= page_entries_info @users
= will_paginate @users
= link_to 'New member', new_admin_user_path
